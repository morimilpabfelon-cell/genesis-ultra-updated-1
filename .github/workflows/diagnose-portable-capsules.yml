name: Diagnose Portable Capsules

on:
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: |
          python -m pip install --disable-pip-version-check -r requirements.txt
          npm ci --ignore-scripts
      - name: Run clean suite and capture output
        shell: bash
        run: |
          set +e
          rm .github/workflows/diagnose-portable-capsules.yml
          npm test > portable-capsule-suite.log 2>&1
          status=$?
          echo "suite_exit=$status" >> portable-capsule-suite.log
          exit 0
      - name: Upload diagnostic log
        uses: actions/upload-artifact@v4
        with:
          name: portable-capsule-suite-log
          path: portable-capsule-suite.log
