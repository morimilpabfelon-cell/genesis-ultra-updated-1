name: Finalize Audit Tool Registry

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  finalize-and-test:
    if: github.head_ref == 'agent/audit-tool-execution-registry'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Prepare final audit integration
        run: python tools/prepare_audit_tool_registry.py

      - name: Commit final state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No generated changes"
          else
            git commit -m "Finalize audit tool execution registry"
            git push origin HEAD:${{ github.event.pull_request.head.ref }}
          fi

      - name: Install pinned Python dependencies
        run: python -m pip install -r requirements.txt

      - name: Install locked Node dependencies
        run: npm ci

      - name: Run complete conformance suite
        run: npm test
